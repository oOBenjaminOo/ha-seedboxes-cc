{
  "config": {
    "abort": {
      "already_configured": "This seedbox is already configured."
    },
    "error": {
      "auth": "Unable to access the seedbox. Check the seedbox ID and session cookie."
    },
    "step": {
      "user": {
        "title": "Connect to Seedboxes.cc",
        "description": "Enter the seedbox ID and the value of the `session_id` cookie from your authenticated Seedboxes.cc dashboard session.",
        "data": {
          "seedbox_id": "Seedbox ID",
          "session_cookie": "Session cookie"
        }
      }
    }
  },
  "options": {
    "step": {
      "user": {
        "data": {
          "scan_period": "Refresh interval (seconds)",
          "sensor": "Enable sensors"
        }
      }
    }
  }
}
